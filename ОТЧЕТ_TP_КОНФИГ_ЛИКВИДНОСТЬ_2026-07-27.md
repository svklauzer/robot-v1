# TP, чистка конфига, ликвидность + разбор Render env

## 1. TP: моя вчерашняя рекомендация оказалась НЕВЕРНОЙ

Вчера я написал: «сдвиг TP1 к 0.5–0.6% делает его достижимым у половины сделок
вместо 19%». Прогнал это на реальных траекториях — **и вывод не подтвердился**.

| TP1 | net (с ярусом 2) | TP1 достигнут |
|---|---|---|
| 0.40% | −3.83 | 10/16 |
| 0.50% | −2.78 | 7/16 |
| 0.60% | −2.23 | 5/16 |
| 0.80% | −2.53 | 1/16 |
| **1.17% (сейчас)** | **−1.76** | 1/16 |

Сдвиг TP1 вниз **ухудшает** результат. Причина — во взаимодействии, которого
вчера ещё не существовало:

| TP1 | с ярусом 2 | БЕЗ яруса 2 |
|---|---|---|
| 0.60% | −2.23 | −3.18 |
| 1.17% | **−1.76** | −4.51 |

Без `trend_capture_band` ранний TP1 действительно помогал — это и была логика
«TP недостижим». Но ярус 2 я починил вчера же, и он закрывает ту же дыру
**лучше**: трейл забирает те же сделки выше, а ранний TP1 срезает половину
позиции до того, как трейл успеет отработать.

Устойчивость проверена: **4 из 5 подвыборок** за текущий TP1 (вся выборка, без
двух крупнейших, вторая половина, только MFE ≥ 0.5).

**Вывод: TP1 не трогаю.** Дыра, ради которой предлагался сдвиг, уже закрыта
другим механизмом. Хорошо, что проверили на данных, а не внедрили по логике.

## 2. Конфиг: удалено 17 мёртвых ключей

Было 476 → стало 459. Удалены ключи, которые **нигде не читаются** (проверено
поиском по всему коду, фронту и инфраструктуре):

```
ADAPTIVE_TRAIL_MFE_START_PCT      FAILED_SETUP_MFE_SOFT/MID/DEEP_PCT
ENABLE_TRAILING_STOP              TRAILING_AFTER_TP1  TRAILING_CALLBACK_PCT
FUNDING_ARB_MIN_EDGE_PCT          FUTURES_MARGIN_MODE  GRID_SLIPPAGE_PCT
MIN_PROTECTIVE_R_MULT             MIN_TREND_CONTINUATION_SCORE
MIN_TREND_STRUCTURE_SCORE         MIN_TREND_STRUCTURE_SCORE
PROD_GATE_{A,A_PLUS,B}_MIN_RR_TP2_PAPER               TELEGRAM_SIGNALS_CHAT_ID
```

Показательно: `ADAPTIVE_TRAIL_MFE_START_PCT` был **изменён** коммитом 4df5092
(1.3 → 0.9) — правился параметр, который ничего не делает. Такие ключи создают
ложное ощущение управления.

Инфраструктурные (`POSTGRES_*`, `NEXT_PUBLIC_API_URL`, `CORS_ORIGINS`,
`DATABASE_URL`) оставлены — они используются в compose и CI.

## 3. Разбор вашего Render env

### Критичное — то, что вы пометили вниманием

**`HTX_MARKET_TYPE=spot` — не влияет.** Проверил симуляцией вашего env:

```
HTX_MARKET_TYPE          = spot
ENABLE_FUTURES_EXECUTION = True
execution_market_type    = swap    ← что реально используется
round-trip               = 0.150%  ← сходится с фактом телеметрии
```

`execution_market_type` = `"swap" if ENABLE_FUTURES_EXECUTION else MARKET_TYPE`.
Комиссии считаются по swap-ставке и **верно**. Но ключ вводит в заблуждение:
выглядит как «мы на споте», хотя торгуем перпы. Стоит убрать из env.

**`HTX_API_HOSTNAME=api-aws.huobi.pro`** — корректно и, вероятно, полезно: это
AWS-эндпоинт. Мой список фолбэков его содержит, ротация при сбое сработает.

### Мёртвые ключи в вашем env

| ключ | статус |
|---|---|
| `BREAKEVEN_GIVEBACK_LOCK_ENABLED=true` | **в config.py такого ключа НЕТ вообще** — молча игнорируется (`extra="ignore"`). Отмечалось ещё 09.07, всё ещё там |
| `NEWS_ENABLED=true` | объявлен в render.yaml, но кодом не читается |
| `REDIS_URL` | то же — объявлен, не используется |
| `HTX_MARKET_TYPE=spot` | перекрывается `ENABLE_FUTURES_EXECUTION` |

### Несоответствия

**~~`FUNDING_ARB_SYMBOLS` привести к торгуемой вселенной~~ — РЕКОМЕНДАЦИЯ ОШИБОЧНА,
снята.**

У внутрибиржевого funding-arb (spot long + perp short) **свои критерии отбора**:
там важны funding rate и ликвидность обеих ног, а не пригодность для
направленной торговли. DOGE и SUI могут быть отличными кандидатами для
дельта-нейтральной стратегии, не входя в торговую вселенную.

Это даже задокументировано в самом конфиге рядом с ключом: «критерий тут НЕ тот
же, что у HTX_SYMBOLS». Я это прочитал и всё равно дал неверный совет.

Ключ читается через property `funding_arb_symbols` — в уточнённом аудите он в
мёртвых не значился и удалён не был.

**`RISK_EQUITY_USDT=950`**, а `robot_loop` передаёт `balance_usdt=1000`
жёстко — отсюда позиция 130 (1000 × 0.13), а не 123.5. Расхождение小ое, но
это две разные цифры эквити в одной системе.

## 4. Ликвидность: ARB убран

Замер спреда против типичного хода (медиана MFE тренда 0.49%):

| символ | медиана спреда | % от хода | |
|---|---|---|---|
| BTC | 0.0000% | 0.0% | ок |
| ETH | 0.0005% | 0.1% | ок |
| XRP | 0.0009% | 0.2% | ок |
| TRX | 0.0024% | 0.5% | ок |
| SOL | 0.0094% | 1.9% | ок |
| AVAX | 0.0175% | 3.6% | ок |
| ADA | 0.0178% | 3.6% | ок |
| **ARB** | **0.3652%** | **74.5%** | **неликвид** |

Разрыв на порядок. ARB дважды блокировался `depth_spread_too_wide` 27.07 и ранее
был дисквалифицирован из cross-arb как «шум тонкой пары».

Убран из дефолтной вселенной. **В вашем `HTX_SYMBOLS` его тоже нужно убрать** —
env перекрывает дефолт.

Честная оговорка: исторически ARB дал +6.28 USDT, но это три сделки, из них
одна CRT с MFE 9.16% — выброс, а не доказанный edge. Спред же измерим и
структурен.

## Верификация

- Полный пакет: baseline (HEAD) **16 падений** → **12 / 267 passed**.
  Новых падений **0**. Конфиг загружается, 459 ключей.

## Что нужно сделать вам в Render env

```
# убрать (мёртвые / вводят в заблуждение)
BREAKEVEN_GIVEBACK_LOCK_ENABLED
HTX_MARKET_TYPE
NEWS_ENABLED          # либо реализовать функционал
REDIS_URL             # либо подключить

# поправить
HTX_SYMBOLS=BTC/USDT,ETH/USDT,SOL/USDT,XRP/USDT,AVAX/USDT,TRX/USDT,ADA/USDT
FUNDING_ARB_SYMBOLS   # привести к торгуемой вселенной или убрать движок
```

## Про перенос настроек в UI

Идея правильная, но перед ней стоит развести ключи на три класса, иначе UI
унаследует ту же кашу:

1. **Секреты и инфраструктура** — в UI не выносить (ключи бирж, БД, токены).
2. **Тумблеры движков** — `ENABLE_*`, вселенные символов, режимы. Это то, что
   действительно нужно в UI.
3. **Калибровочные пороги** — `TREND_CAPTURE_*`, `OB_*`, `SYMBOL_PERF_*`.
   Их **не** стоит давать в UI как свободные поля: каждый выведен из замера, и
   произвольная правка молча ломает калибровку. Максимум — показывать значение,
   источник (какой замер) и дату, с редактированием через отдельный «режим
   эксперта».

Иначе получится то же самое, что с `4df5092`: параметры поменяли, обоснование
не записали, регресс нашли через две недели.
