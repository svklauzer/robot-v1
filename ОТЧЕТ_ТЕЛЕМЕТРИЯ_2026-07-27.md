# Разбор телеметрии 27.07: что сработало, что нет, три новые течи

## Сначала — что подтвердилось

**Гейты честности работают.** `validation-gates` показывает сырой PnL **+9.89**
и честный **−3.37**: два фантомных филла (#281, #272) завышают окно на
**13.26 USDT**. Live заблокирован автоматически — ровно то поведение, ради
которого гейт добавлялся. Ни одного НОВОГО фантома не появилось
(`exit-replay`: `phantom_fill_trades: 0`).

**Сеть в порядке.** `egress-history`: 523 замера за сутки, доступность **100%**,
окон недоступности **0**, худший DNS 21.3 мс. Инцидент 26.07 не повторялся.

---

## Течь 1: новый ярус фиксации не сработал НИ РАЗУ

Причины закрытий за 72ч: `breakeven_lock` ×4, `stop_loss`, `breakeven_stop`,
`trend_ride_trailing_stop`. `trend_capture_band` — **ноль**.

Разбор показал, что порог вооружения задаёт **не параметр `arm`, а пол**:

```
нужен MFE >= floor / (1 - give) = 0.40 / 0.75 = 0.533%
```

А медиана MFE новых сделок упала до **0.489%** (в калибровке была 0.641%).
Полоса [0.533, 0.80) оказалась пустой:

| сделка | MFE | закрылся на | net | почему молчал |
|---|---|---|---|---|
| #287 ADA | 0.430% | 0.225% | +0.03 | ниже порога 0.533 |
| #286 TRX | 0.489% | 0.182% | +0.08 | ниже порога 0.533 |
| #285 SOL | 0.467% | 0.144% | −0.02 | ниже порога 0.533 |

Все три ушли в безубыток-замок около нуля, отдав почти весь пик.

**Исправлено:** у яруса 2 теперь СВОЙ пол `TREND_CAPTURE_FLOOR_PCT = 0.30`
(= net_safe для swap, ниже фиксировать нечего), `arm` снижен 0.55 → 0.40, чтобы
порог задавал именно пол. Новый порог вооружения — **0.40% MFE**.

Прогон боевого движка по реальному откату этих сделок: #286 **+0.53** вместо
+0.08, #285 **+0.63** вместо −0.02. #287 режет $-гейт на марже 125 — это
корректно, фиксация 0.32% там даёт меньше минимума.

Замер: новая выборка **+1.1**, калибровочная **−0.41** (ярус иногда перехватывает
будущего раннера у ride) → суммарно **+0.7**.

## Течь 2: сетка сливает в разогнанном рынке

Grid realized ушёл с −4.15 до **−6.20**; за трое суток **−2.05**. Разбор 12
циклов дал разделение по RSI **без единого исключения**:

| исход | RSI | сумма |
|---|---|---|
| все 3 убытка (`grid_stop_loss`) | 76.2 / 83.1 / **87.0** | **−2.20** |
| все 3 прибыли | 50.6 / 66.0 / 69.9 | +0.15 |

Механика: «нейтральная» сетка в одностороннем ходе набирает **только
контр-сторону** — в обоих ETH-циклах заполнились все sell-уровни, цена ушла
вверх, корзина поймала стоп. Это не чоп, для которого сетка предназначена.

**Исправлено:** `GRID_HTF_EXTREME_VETO` — не строим цикл при RSI ≥ 72 или ≤ 28.
Пороги те же, что у `TREND_HTF_RSI_HARD_OVERHEAT`. Сетка была **третьим**
движком с этой болезнью: у скальпа и тренда её уже лечили тем же способом.

На выборке вето снимает весь убыток (−2.20) и **не режет ни одной прибыльной
корзины** — это зафиксировано тестом.

## Течь 3: дневной отчёт показывал прибыль, которой нет

`daily-quality-report` за 72ч: **net_pnl +5.60 USDT**. Честно — **−0.94**:
в окне лежит фантомный #281 (+8.18 против реальных +1.64).

Отчёт питался только от `closed_net_pnl`, тогда как `validation-gates` рядом уже
судил по честному. Владелец видел зелёную цифру рядом с красным гейтом.

**Исправлено:** отчёт считает `net_pnl_honest_usdt`, отдаёт счётчик фантомов,
и порог «negative net PnL» срабатывает по честной цифре.

---

## Найдено, но НЕ менял — нужно ваше решение

### Cross-arb (P2): 8 закрытий, прибыльных ноль

| символ | спред входа | realized |
|---|---|---|
| TRX | 18.88% | −0.200 |
| AVAX | 17.46% | −0.189 |
| SOL | 16.81% | −0.211 |
| TRX | 15.61% | −0.180 |
| XRP | 29.35% | −0.080 |
| AVAX | 25.26% | −0.060 |
| SOL | 13.41% | −0.120 |
| XRP | 15.58% | −0.156 |

**−1.20 USDT, 0 из 8 в плюс, все по `spread_flipped`.** Carry не успевает отбить
комиссии (0.20 на ногу) — даже с confirm-фиксом от 22.07.

Причина видна в истории спредов: **вселенная устарела**.

| символ | avg спред | устойчивость | статус |
|---|---|---|---|
| **ADA** | **24.12%** | **97.6%** | **не входит во вселенную** |
| XRP | 9.88% | 81.0% | торгуется |
| AVAX | 14.63% | 77.8% | торгуется, ниже критерия |
| SOL | 12.40% | 72.2% | торгуется, ниже критерия |
| TRX | −1.65% | 54.8% | торгуется, **знак развернулся** |

TRX, по которому P2 включали (16.5% @ 95.2%), теперь **−1.65% @ 54.8%**.
Единственный символ, проходящий собственный критерий движка (≥12% @ ≥80%) —
ADA — в его вселенной отсутствует.

**Варианты:** добавить ADA в `CROSS_FARB_SYMBOLS` и убрать TRX; либо
переключить вселенную на динамическую (брать топ по устойчивости из истории
P1); либо выключить P2 до пересмотра. Менять состав торгуемых инструментов без
вашего решения не стал.

### Входы без подтверждения стаканом

ADA #288 — **единственный** вход периода с `entry_depth.fresh = false` — дал
**−3.26 USDT** (stop_loss), то есть весь минус окна. В те же часы depth-гейт
десятки раз блокировал ADA-шорты с OBI 0.52–0.89 против входа: данные были и
были против, просто в момент входа фид молчал.

Fail-open здесь осознан (движок не должен вставать из-за обрыва WS), но это
**молчаливое отключение проверки** — а такие уже дважды оказывались дорогими
(min_amount, precision). Одного случая мало, чтобы менять торговую логику,
поэтому добавил:

- метку `no_depth_data:unverified` в решении гейта;
- **`GET /analytics/depth-coverage`** — сравнение результатов входов с
  подтверждением и без. Через неделю по нему можно будет решать на статистике.

### positive_then_negative 56% при пороге 25%

Не двигалось. Это структурная характеристика: WR 34.6%, победители редкие и
крупные, хвосты режутся мелкими. Гейт откалиброван под другой профиль
распределения и, возможно, сам требует пересмотра — но это отдельный разговор.

---

## Верификация

- Полный пакет: baseline (HEAD) **16 падений** → **12 / 236 passed**.
  Новых падений **0**.
- Новые тесты: HTF-вето сетки (6 — включая проверку, что вето снимает весь
  убыток выборки и не режет ни одной прибыльной корзины, и регресс на то, что
  гейт стоит именно в пути открытия цикла).
- Обновлён тест пола яруса 2 под собственный порог.
- Фронт: esbuild — 18 файлов, ошибок 0.

## Ожидания к следующему разбору

1. `trend_capture_band` должен появиться в причинах закрытий. Если снова ноль —
   значит распределение MFE сместилось ещё ниже, и вопрос уже не в пороге, а в
   том, что сетапы не доходят до цели.
2. Grid: новых `grid_stop_loss` при RSI > 72 быть не должно.
3. `net_pnl_honest_usdt` в дневном отчёте — сравнивать только с ним.
4. `depth-coverage` — накопить выборку, решить по fail-open.
5. Фантомы #281/#272 выкатятся из окна валидации — тогда `rolling_net_pnl`
   впервые покажет правду без поправки.
