# Аудит 2026-07-25: фантомные филлы, убыточный «безубыток», сломанный кулдаун

## Главный вывод

**Система не зарабатывала. Она рисовала прибыль ценами, которых рынок не видел.**

Два крупнейших «выигрыша» в истории — TRX #272 (+10.97) и TRX #281 (+8.18), вместе
+19.15 USDT — закрыты по цене `entry × 1.018`, то есть ровно на
`MIN_PROTECTIVE_EXIT_PCT = 1.80%`, независимо от рынка. Обе цены **выше максимума,
который сделка вообще видела** (`max_profit_price`). Честный результат тех же двух
сделок — +3.92 USDT. Завышение: **−15.23 USDT**.

Ретроспектива 24.07 писала «10–23 июля ≈ +2.4, впервые устойчиво около нуля с
положительным знаком». Убрав фантом из этого окна, получаем **≈ −12.8**. Вывод
«течи убраны» был основан на несуществующей прибыли.

---

## Баг 1 — фантомная цена выхода (критический)

`services/exit_policy.py`, 7 веток защиты/трейла:

```python
protected_pct = max(mfe * (1.0 - ride_trail_dd), net_safe_pct, min_protective_exit_pct)
exit_price    = self._price_from_result_pct(side, entry_price, protected_pct)   # ← филл
```

`net_safe_pct` и `MIN_PROTECTIVE_EXIT_PCT` — **экономические гейты** («не стоит
фиксировать дешевле»). Через `max()` они попадали прямо в **цену исполнения**.
Стоп/трейл при этом не может исполниться лучше рынка — а здесь мог.

Доказательство (арифметика сходится до 8-го знака):

| сделка | entry | `entry×1.018` | филл в проде | max за сделку | рынок |
|---|---|---|---|---|---|
| TRX #281 | 0.328645 | 0.33456061 | **0.33456061** | 0.331845 | 0.330223 |
| TRX #272 | 0.326736 | 0.33261725 | **0.33261725** | 0.33000 | 0.328347 |

Худшая ветка — `protective_breakeven_profit_guard`: срабатывает при
`current_pct <= net_safe_pct` (сделка **на нуле или в минусе**) и книжит выход
на +1.80%.

**Последствия шире PnL:** `validation_gates`, `rolling net PnL`, `capture_rate`,
`exit-replay` («текущий конфиг №1 шесть замеров подряд») и метки `is_win`
ML-датасета питались этой цифрой. На live такой ордер просто не исполнится —
реальный филл будет по рынку, то есть в 4–5 раз хуже.

**Фикс:** helper `_achievable_pct(protected, current) = min(protected, current)`
применён во всех 7 ветках (`_result_pct` уже нормализует знак → корректно для
long и short). Триггеры выходов **намеренно не тронуты** — изменена только цена,
чтобы телеметрия стала честной без незавалидированной смены стратегии.

## Баг 2 — «безубыток-замок» гарантированно закрывался в минус

`BREAKEVEN_LOCK_FLOOR_PCT = 0.10`, фактический round-trip — **0.15%** нотионала.
Выход по правилу `current_pct <= 0.10` → net = `0.10 − 0.15` = **−0.05%** всегда.

Проверено на 7 из 20 последних закрытий — формула `net = gross − 0.15` сходится
точно: #282 −0.245, #279 −0.126, #275 −0.209, #273 −0.124, #270 −0.135,
#268 −0.107, #267 −0.166. Итого −1.11 USDT, и каждая отдала MFE 0.36–0.78%.

Причина регресса: коммит **`4df5092` от 17.07 «КОРР ПАРАМЕТРОВ ДИП РЕКОМЕНД ЭКСПЕР»**
откатил фикс `#leak-be-lock` от 09.07 (0.15 → 0.10). Тем же коммитом молча
вернулись `EXIT_REQUIRE_FLOW_CONFIRM: True → False` и
`FAILED_SETUP_EXIT_TREND_ENABLED: False → True` — обе настройки аудит 09.07
менял намеренно и с обоснованием.

**Фикс:** эффективный пол = `max(конфиг, round_trip + BREAKEVEN_LOCK_COST_BUFFER_PCT)`,
считается от фактической ставки комиссии, а не константой. Плюс защита
`arm >= floor × 1.2`, чтобы замок не вырождался в выход по шуму.
Те же 7 сделок: было −1.11 USDT → стало **+0.31 USDT**.

## Баг 3 — cross-arb: «никогда не закрывался» = «закрыт в эпоху 0»

`services/cross_funding_arb.py`: `last_closed_ts.get(s, 0.0)` — sentinel `0.0`
означал «закрыт в 1970». Условие `now - 0.0 >= cooldown` держалось только потому,
что `now` — большой unix-timestamp. При любом малом/относительном `now` движок
**молча не открывал ни одной позиции**.

Это ровно тот класс ошибки, что уже ловили в `opened_ts or now_ts`. Он же держал
**5 тестов `test_cross_funding_arb.py` красными** — те самые, что были написаны
22.07 и не прогнаны («песочница недоступна»).

**Фикс:** sentinel `None`, кулдаун применяется только к реально закрывавшимся
символам. 5/5 тестов зелёные.

---

## Верификация

- Полный пакет: baseline (HEAD) **21 failed / 166 passed** → после правок
  **12 failed / 177 passed**. Новых падений — **0**. Починено 5 (cross-arb);
  остальные 12 — pre-existing контрактные (требуют полного дерева репо/web).
- `test_exit_policy` + `test_trade_plan` + `test_cross_funding_arb` +
  `test_venue_compare`: **36/36**.
- Заменён тест `test_before_tp1_protective_breakeven_uses_v4_profit_floor`: он
  **закреплял баг** (`assert exit_price >= 101.8` при рынке 100.55). Теперь
  проверяет обратное. Добавлены 2 теста: филл не лучше рынка (long+short) и
  пол замка выше round-trip издержек.
- Реплей обеих TRX-сделок через исправленную политику: филл = рынок
  (0.330223 / 0.328347), net 1.64 / 2.28 вместо 8.18 / 10.97.

## Что требует решения владельца (не менял)

1. **Рекалибровка гейтов.** `MIN_PROTECTIVE_EXIT_PCT = 1.80` и
   `MIN_PROTECTIVE_NET_USDT = 2.50` калибровались, когда были *ценой*. Теперь
   это чистые гейты, и при типичном MFE 0.4–0.8% они почти всегда блокируют
   защитный выход → сделка едет до стопа. Нужен пересчёт под реальное
   распределение MFE.
2. **Мёртвая зона MFE 0.35–0.8% в тренде.** Ветка `ride` делает ранний
   `return ExitDecision(exit=False)` — `adaptive_mfe_capture` в тренде
   **недостижим**. При `TREND_RIDE_MIN_MFE_TO_PROTECT_PCT = 0.8` сделки с MFE в
   этой полосе (а `avg_mfe` trend_up = 0.798%) не имеют ни одного механизма
   фиксации, кроме замка. Это структурная дыра ровно в модальном случае.
3. **Перевалидация после честного PnL.** `validation_gates`, `exit-replay` и
   ML-метки нужно перечитать заново — прежние вердикты опирались на фантом.
4. **Ревизия коммита `4df5092`** целиком: он откатил три обоснованных решения
   аудита 09.07 без записи в отчёт.
5. **Чурн на AVAX 25.07:** #282 закрыт замком в 00:51 по 6.2708, #283 открыт в
   01:54 по **6.2751** — дороже, тот же символ и сторона. Round-trip оплачен
   дважды. `reentry_cooldown` (60 мин) отработал формально, но не защищает от
   перезахода по худшей цене.
